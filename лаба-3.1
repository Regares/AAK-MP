#include <stdio.h>

int main()
{
    float a, b, S, P;
    printf("Введите длину и ширину прямоугольника через пробел: ");
    scanf("%f%f", &a, &b);
    S = a * b;
    P = 2 * (a + b);
    printf("Прощадь S равна: %f    Периметр равен: %f  ", S, P);
    return 0;
}
