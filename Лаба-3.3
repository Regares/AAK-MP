#include <stdio.h>

int main()
{
    float a, b, res;
    printf("Введите два числа через пробел: ");
    scanf("%f%f", &a, &b);
    res = (a + b) / 2;
    printf("Среднее арифметическое этих чисел равно: %f  ", res);
    return 0;
}
